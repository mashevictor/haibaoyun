若双击 .bat 仍闪退或报「不是内部或外部命令」、乱码，请按下面做一次：

1. 右键「测试窗口是否闪退.bat」 -> 用「记事本」打开
2. 菜单选「文件」->「另存为」
3. 下方「编码」选「ANSI」（不要选 UTF-8）
4. 保存，覆盖原文件
5. 再双击该 bat 试一次

若这样能正常显示、不闪退，再对「一键启动网站.bat」和「直接打开网页.bat」做同样操作（记事本打开 -> 另存为 -> 编码选 ANSI -> 保存）。
